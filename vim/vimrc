
" Load from vim/

let b:fileList = split(globpath('~/.vim', 'plugins.vim'), '\n')
let b:fileList += split(globpath('~/.vim/base', '**/*.vim'), '\n')
let b:fileList += split(globpath('~/.vim/plugins', '**/*.vim'), '\n')

for fpath in b:fileList
  exe 'source' fpath
endfor
